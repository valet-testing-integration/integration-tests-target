name: begonaguereca/cron-jenkinsile
on:
  schedule:
  - cron: 12 */4 * * 1-5
  - cron: 48 */4 * * 1-5
jobs:
  Example:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v2
    - name: echo message
      run: echo Hello World
